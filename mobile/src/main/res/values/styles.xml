<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Config.Text" parent="TextAppearance.AppCompat.Medium">
        <item name="android:padding">15dp</item>
    </style>

    <style name="AppTheme.Config.Title" parent="TextAppearance.AppCompat.Medium">
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingRight">15dp</item>
        <item name="android:paddingTop">15dp</item>
        <item name="android:paddingBottom">2dp</item>
    </style>

    <style name="AppTheme.Config.Description" parent="TextAppearance.AppCompat.Small">
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingRight">15dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">15dp</item>
    </style>

</resources>
